node{
	stage('scm checkout'){
	git 'https://github.com/sureshkj/maven-project.git'
	}
	stage('package'){
	def mavencmd = tool name: 'localmaven', type: 'maven'
	sh "${mavencmd}/bin/maven package"
	}
}